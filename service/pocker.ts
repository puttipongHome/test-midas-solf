const pocker = {
  patient: async (data: any) => {
    const results = data;
    return results;
  },
};

const slave = {
  patient: async (data: any) => {
    const results = data;
    return results;
  },
};

const controller = {
  pocker,
  slave,
};

export default controller;
